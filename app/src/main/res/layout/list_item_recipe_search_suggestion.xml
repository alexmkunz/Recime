<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools">

<LinearLayout
        android:id="@+id/list_item_recipe_search_suggestion"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/searchSuggestion">

    <TextView
            android:id="@+id/list_item_recipe_search_suggestion_recipe_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/shape_bottom_border"
            android:padding="@dimen/spacing_normal"
            android:textColor="@color/searchSuggestionTitle"
            android:textSize="@dimen/font_normal"
            tools:text="Recipe Name" />
</LinearLayout>

</layout>